groups:

  - name: basic
    rules:
      - alert: InstanceDown
        expr: up == 0
        for: 2m

        labels:
          severity: critical

        annotations:
          summary: "Instance down"
          description: "{{ $labels.instance }} is unreachable"
