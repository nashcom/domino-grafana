Add a certificate and key into this directory for NGINX TLS

- cert.pem
- key.pem


Note: If the key is encrypted you can use the following command-line:

```
openssl pkey -in encrypted_key -out key.pem
```

